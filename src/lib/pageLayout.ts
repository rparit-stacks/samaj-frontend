/**
 * Default content shell for AppLayout pages — horizontal inset + max width + bottom padding for mobile nav.
 * Aligns with News (`p-4 md:p-6`) / Dashboard (`max-w-6xl` …) patterns without full-bleed edge content.
 */
export const APP_PAGE_CONTAINER =
  "w-full max-w-3xl mx-auto min-w-0 px-4 pt-3 pb-24 md:max-w-4xl md:px-6 md:pt-4 md:pb-10";
