<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Android 15 (API 35) forces edge-to-edge; without this opt-out,
      statusBarColor / decorFitsSystemWindows are ignored and content
      draws under a translucent (often pure-white) status bar.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:statusBarColor">@color/systemBar</item>
        <item name="android:navigationBarColor">@color/systemBar</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:statusBarColor">@color/systemBar</item>
        <item name="android:navigationBarColor">@color/systemBar</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>
</resources>
